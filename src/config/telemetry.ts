// General application modules import this compatibility surface. The
// implementation lives in the bootstrap-safe module so opt-out and persisted
// state semantics have one authoritative source.
export * from "./telemetry-state.js";
